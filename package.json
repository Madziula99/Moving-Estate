{
  "name": "moving-estate",
  "private": true,
  "scripts": {
    "install:server": "cd server && yarn install",
    "install": "yarn install:server",
    "start:server": "cd server && yarn start"
  },
  "dependencies": {
    "config": "^3.3.8",
    "proxy": "^1.0.2"
  }
}
